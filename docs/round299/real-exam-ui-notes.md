# Round299 Real-Exam UI Notes

Worker D made a compact student-facing count-boundary strip in `modules/real-exams-dynamic.html`.

- The hero and source-granularity panel now state the original-source count boundary as `316` original atomic questions, `61` grouped source sections, and `201` grouped subquestions that are expanded instead of merged.
- The short-answer example is kept concrete with `2010 简答题×5`, so students can see that a five-item short-answer parent is counted as five practice items.
- The answer evidence boundary remains explicit: question-source proof does not mean answer-PDF verbatim proof, and current answers stay in the reference/derived layer.

No data JSON or large scripts were rewritten in this lane.
